class User {
    id
    name
    selectedCard

    constructor(id, name, selectedCard) {
        this.id = id;
        this.name = name;
        this.selectedCard = selectedCard;
    }
}

export default User;